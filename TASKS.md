# Tasks

Задачи сформированы по результатам аудита проекта от 2026-09-12. Приоритеты:

- **P0** — риск повреждения данных, компрометации или полностью неработающий основной сценарий.
- **P1** — существенная функциональная или эксплуатационная проблема.
- **P2** — важное улучшение качества, воспроизводимости или сопровождения.

## Безопасность и целостность

- [x] 1. **P0** Защитить производные пути сборки. Валидировать `package`, `version` и `architecture`, разрешать операции только внутри явно заданных `base-dir` и `tmp-dir`, проверять разрешённый корень и принадлежность каталога перед `os.RemoveAll`. Использовать `os.Root`. Не допускать пустой `package` и path traversal. См. `pkg/config/config.go:56-95` и `internal/commands/build.go:43-48`.
- [x] 2. **P0** Заменить `Release.gpg`, который сейчас содержит публичный ключ, на настоящую detached OpenPGP-подпись файла `Release`. Проверить подпись командой `gpg --verify` и добавить автоматический тест.
- [x] 3. **P0** Убрать зависимость от небезопасного и неподдерживаемого `golang.org/x/crypto/openpgp` (`GO-2026-5932`). Перейти на поддерживаемый механизм подписи либо изолировать внешний `gpg`/`sq` с безопасным управлением ключами.
- [x] 4. **P1** Ограничить размер извлекаемого control-файла и заменить неограниченный `io.ReadAll` в `pkg/archive/reader.go:48-56` на чтение с лимитом.
- [x] 5. **P1** Исправить утечку файлового дескриптора при ошибке `gzip.NewReader` в `pkg/archive/reader.go:21-31`; проверить закрытие всех ресурсов на ошибочных путях.

## Сборка пакетов

- [x] 6. **P0** Исправить MD5 для `WriteData`: `md5.New().Sum(b)` не хеширует данные, а добавляет MD5 пустого сообщения к исходному буферу. Добавить регрессионный тест с настоящим MD5. См. `pkg/archive/writer.go:56-78` и ошибочное ожидание в `pkg/archive/writer_test.go:24-31`.
- [x] 7. **P1** Исправить формат описания Debian-пакета: каждая перенесённая строка должна начинаться с пробела; валидировать результат через `dpkg-deb`/парсер control-файлов. См. `pkg/control/control.go:73-92`.
- [x] 8. **P1** Сделать валидацию YAML строгой: отклонять неизвестные версии схемы, неизвестные поля и конфигурации без обязательных полей вместо успешного no-op. См. `pkg/config/config.go:71-99`.
- [x] 9. **P1** Сделать проверку Debian-версии полной и соответствующей реальному формату: текущий regexp не заякорен, допускает лишние символы и одновременно отклоняет допустимые версии без epoch.
- [x] 10. **P1** Устранить гонку при выборе имени revision-пакета: резервировать имя атомарно или добавить межпроцессную блокировку вокруг проверки и создания `.deb`. См. `pkg/packages/name.go:32-59`.
- [x] 11. **P2** Сделать сборку воспроизводимой: сортировать `data`, убрать `time.Now()` из tar-записей или задавать стабильное время, исправить сортировку пакетов в release-индексе.
- [x] 12. **P2** Обрабатывать ошибки `Close` в `pkg/archive/gzwriter.go:14-25`; не возвращать успех при незавершённой gzip-записи или ошибке flush/close.

## Release-репозиторий

- [ ] 13. **P0** Синхронизировать имя control-записи: builder пишет `control`, а release ищет `./control`. Использовать нормализацию пути или единый формат. См. `internal/commands/build.go:183-190` и `internal/commands/release.go:91-98`.
- [ ] 14. **P1** Исправить фильтрацию файлов в `filepath.Walk`: обрабатывать только обычные файлы с расширением `.deb`. Сейчас любой README, временный файл или `.DS_Store` передаётся в `ar.Open`. См. `internal/commands/release.go:72-84`.
- [ ] 15. **P1** Генерировать корректные Release-метаданные: использовать значения `dist` и `comp`, не хардкодить `stable`, `main` и архитектуры, добавить стандартное поле `Components` и необходимые поля Suite/Codename. Проверить результат через APT.
- [ ] 16. **P1** Не терять поддерживаемые Debian-архитектуры: либо валидировать и явно документировать список, либо индексировать все допустимые архитектуры вместо молчаливого отбрасывания неизвестных. См. `internal/commands/release.go:35` и `internal/commands/release.go:141-148`.
- [ ] 17. **P2** Публиковать Release-файлы атомарно, чтобы APT не увидел смесь старых и новых `Packages`, `Release` и подписей.
- [ ] 18. **P2** Исправить режимы файлов метаданных: `Packages`, `Release`, `InRelease`, `Release.gpg` и `key.gpg` не должны создаваться с `0755`.

## Архитектура и тестирование

- [ ] 19. **P1** Добавить end-to-end тест: собрать `.deb`, распаковать его через `dpkg-deb`, проиндексировать командой `release`, проверить `Packages`, `Release`, `InRelease`, detached-подпись и загрузку локального репозитория через APT.
- [ ] 20. **P1** Добавить тесты для malformed gzip/tar, отсутствующего `control`, traversal-имён, symlink-файлов, больших control-записей, повторного чтения и закрытого reader.
- [ ] 21. **P2** Исправить `TGZReader.Reset`: сейчас gzip-reader сбрасывается на tar-reader вместо исходного файла. См. `pkg/archive/reader.go:44-46`.
- [ ] 22. **P2** Разделить оркестрацию CLI и бизнес-логику сборки/release, чтобы основную логику можно было тестировать без `console.FatalIfErr` и `os.Exit`.
- [ ] 23. **P2** Переименовать публичный тип `PackegesModel` в корректный `PackagesModel` с совместимым переходом, если API уже используется внешними клиентами.

## CI и инструменты

- [x] 24. **P1** Исправить `.PHONY`: объявлен `pre-commite`, но используется `pre-commit`. См. `Makefile:23-24`.
- [x] 26. **P2** Не подавлять глобально проверки `gosec` G204/G304 без компенсирующих проверок путей и shell-команд. См. `.golangci.yml:40-49`.

## Исходная проверка

На момент аудита успешно прошли:

- `go test ./...`
- `go test -race ./...`
- `go test -count=3 ./...`
- `go vet ./...`
- `go mod verify`
- `git diff --check`

`make lint` не завершился: установленный `golangci-lint` собран под более старую версию Go, чем указанная в проекте (`1.23/1.25` против `1.26`). Полноценное покрытие команд `build` и `release` отсутствует.
